# Cloudflare Pages reads this file (it is not served). One rule per line:
# from  to  status, paths only (the host cannot redirect one domain to
# another from here; www serves the same site and every page carries a
# canonical link to the bare domain instead).
